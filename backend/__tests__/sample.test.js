describe("Sample Test Suite", () => {
  test("should pass basic assertion", () => {
    expect(1 + 1).toBe(2);
  });

  test("should verify true is true", () => {
    expect(true).toBeTruthy();
  });
});
